spring.application.name=github-repo-search

# === Database Configuration ===
spring.datasource.url=jdbc:postgresql://localhost:5432/githubdb
spring.datasource.username=vallipichowdappa1
spring.datasource.password=password123

# === JPA Configuration ===
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.open-in-view=false

# === HikariCP Connection Pool Settings (Optional but recommended) ===
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.idle-timeout=600000
spring.datasource.hikari.pool-name=SpringBootHikariCP
spring.datasource.hikari.max-lifetime=1800000
spring.datasource.hikari.connection-timeout=30000

